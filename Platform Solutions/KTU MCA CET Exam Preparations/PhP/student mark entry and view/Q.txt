7)Create a table named student containing fields rollno(Primary key) 
and name.Enter some values into the tables manually(Without using code).

Create a web page which contains two links mark entry and mark view.
When the user clicks on the mark entry then a web page for markentry needs
to be loaded into the target frame. 

Mark entry web page should contain the fields rollno(Drop-down list), 
mark.On clicking the submit button, these values should be saved
into the database.

 When the user clicks on the mark view, 
the entered mark and rollno needs to be displayed in a neat 
format into the target frame.