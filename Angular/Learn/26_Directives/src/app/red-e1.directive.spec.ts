import { RedE1Directive } from './red-e1.directive';

describe('RedE1Directive', () => {
  it('should create an instance', () => {
    const directive = new RedE1Directive();
    expect(directive).toBeTruthy();
  });
});
